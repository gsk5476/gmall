server:
  port: 18084
feign:
  sentinel:
    enabled: true
mybatis-plus:
  mapper-locations: chasspath:mapper/oms/**/*.xml
  type-aliases-package: com.atguigu.gmall.oms.entity
  global-config:
    db-config:
      id-type: auto